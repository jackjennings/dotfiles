eval "$(/opt/homebrew/bin/brew shellenv)"

# The `.zprofile_ext` file is not checked in to version control
# in order to permit customization that is not tracked.
if [ -f ~/.zprofile_ext ]; then
  source ~/.zprofile_ext
fi
