# Set Homebrew environment variables (PATH, MANPATH, HOMEBREW_PREFIX, etc.).
# Kept in .zprofile rather than .zshrc so these are available in non-interactive
# login shells (launchd agents, SSH sessions, zsh -l scripts) — not just
# interactive terminals.
eval "$(/opt/homebrew/bin/brew shellenv)"
